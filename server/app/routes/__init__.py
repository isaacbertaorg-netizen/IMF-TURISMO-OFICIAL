# ===========================================================================
# IMF TURISMO — pacote de rotas
# Define os routers HTTP. A lógica de negócio fica nas camadas de serviço;
# aqui só ocorre validação de entrada (Pydantic), autenticação (Depends) e
# rate limiting (slowapi).
# ===========================================================================
