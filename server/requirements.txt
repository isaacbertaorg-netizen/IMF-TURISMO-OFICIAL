# ===========================================================================
# IMF TURISMO — dependências do back-end (server/)
# Instale com: pip install -r requirements.txt
#
# fastapi/uvicorn    : framework web e servidor ASGI.
# pydantic           : validação de schema de entrada (seção 6.1).
# supabase           : client oficial para Supabase (PostgreSQL + Auth + Storage).
# python-dotenv      : carrega variáveis de ambiente do .env (seção 6.3).
# slowapi            : rate limiting nas rotas públicas sensíveis (seção 6.5).
# httpx              : client HTTP usado pelo TestClient/pytest.
# pytest             : runner da suíte de testes TDD (seção 4).
# ===========================================================================

# --- Runtime ---
fastapi
uvicorn[standard]
pydantic
# Validator de e-mail usado por pydantic EmailStr nos schemas.
email-validator
supabase
python-dotenv
slowapi
# Hash de senha (schema usa senha_hash em cliente/administrador).
bcrypt
# Geração/validação de token JWT de sessão própria.
PyJWT
# Adaptador ASGI->Lambda para rodar a API como serverless no Vercel.
mangum

# --- Testes e desenvolvimento ---
pytest
httpx
# Lint e formatação de código.
ruff
